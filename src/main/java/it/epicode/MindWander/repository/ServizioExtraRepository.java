package it.epicode.MindWander.repository;

import it.epicode.MindWander.model.ServizioExtra;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ServizioExtraRepository extends JpaRepository<ServizioExtra, Long> {
}
